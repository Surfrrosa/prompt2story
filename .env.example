# OpenAI API Key - Required for all AI generation features
# Get your key from: https://platform.openai.com/api-keys
# NEVER commit real API keys to version control
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# AI Model Configuration
# JSON-optimized model for structured responses
JSON_MODEL=gpt-4o-mini

# Text generation model for complex responses
TEXT_MODEL=gpt-4o

# Optional: JSON-friendly vision model; falls back to gpt-4o
JSON_VISION_MODEL=gpt-4o

# Allowed CORS origins for API access
# Comma or space separated list, supports wildcards like https://*.vercel.app
ALLOWED_ORIGINS=https://prompt2story.com,https://*.vercel.app